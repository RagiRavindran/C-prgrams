#include<stdio.h>

int main()
{
    int x = 100003, n=0,d, i=1;
     printf("%d\n", x);
    while(x > 0)
    {
     d=x%10;
     if(d == 0)
        d = 5;
    n = i*d + n;
    x=x/10;
    i *= 10;
    }
    printf("%d\n", n);
    
    return 0;
}
